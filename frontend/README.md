# Clock System Frontend

## Setup

1. Copy `.env.example` to `.env`.
2. Set `VITE_API_BASE_URL` to your backend API base URL (example: `http://localhost:3000/api`).
3. Install dependencies:

```bash
npm install
```

4. Run development server:

```bash
npm run dev
```

## Build

```bash
npm run build
```
